import { makeStyles } from "@mui/material";

const useStyles = makeStyles((theme) => ({
    boldLabel: {
        fontWeight: 'bold',
    },
    customSelect: {
        height: '38px',
    }
}));

export default useStyles;